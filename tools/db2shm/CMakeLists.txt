CMAKE_MINIMUM_REQUIRED(VERSION 3.8)

SET(TARGET_NAME db2shm)
PROJECT(${TARGET_NAME})

SET(CMAKE_CXX_STANDARD 17)

# db2shm
SET(SOURCE_FILES db2shm.cc
    db2shm_opt.cc db2shm_opt.h
)

ADD_EXECUTABLE(${TARGET_NAME} ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(${TARGET_NAME} ${apr})
